# Give your application a name (good for monitoring)
spring.application.name=performance-demo

# Expose the 'prometheus' and 'health' endpoints over HTTP
management.endpoints.web.exposure.include=prometheus,health

# (Optional but good) Add tags to all metrics for better filtering
management.metrics.tags.application=${spring.application.name}